# 導入マニュアル

shogi_with_AI を使い始めるまでの手順。使い方は2通りある。

| | A. claude.ai のプロジェクトで使う（推奨） | B. 手元の Linux で使う |
|---|---|---|
| 向いている人 | AIと会話しながら棋譜を振り返りたい | コマンドで解析結果だけ欲しい |
| 必要なもの | claude.ai のアカウント | Linux（x86-64）または Windows の WSL2 |
| 環境構築 | AIが会話の中で行う | 自分でコマンドを実行する |

どちらの場合も、**棋譜データはこのリポジトリに含まれない**。自分の棋譜（KIF形式）を用意すること。

---

## A. claude.ai のプロジェクトで使う

### A-1. 準備するもの

- claude.ai のアカウント（プロジェクト機能とコード実行が使えるプラン）
- 自分の棋譜（KIF形式。将棋ウォーズ・ぴよ将棋などから書き出したもの）

### A-2. プロジェクトを作る

1. claude.ai で新しいプロジェクトを作る（名前は自由。例：「将棋の振り返り」）
2. claude.ai の設定で、**コード実行**と、**外部への通信（github.com・pypi.org）**を許可する
   - 環境構築で、GitHub から将棋エンジンと評価関数を、PyPI から python-shogi をダウンロードするため
   - 設定の名称や場所は claude.ai の画面によって異なる。通信が許可されていないと、手順 A-5 の環境構築が失敗する

### A-3. プロジェクトの指示を設定する

プロジェクトの「指示（カスタム指示）」に、次の文をそのまま貼る。
AIは毎回の会話で最初にこれを読むので、初回のヒアリングや環境構築が確実に行われる。

```
このプロジェクトでは、公開リポジトリ hassan83/shogi_with_AI の棋譜解析スクリプトを使って、私の棋譜を一緒に振り返る。

会話の最初に、他の作業より先に次を必ず行うこと。
1. Knowledgeに「ユーザープロファイル.md」があるか確認する。
   - ない場合は初回。解析・助言より先に、リポジトリの profile/ユーザープロファイル.md の手順でヒアリングする。
     まとめた結果を私に確認してから、「ユーザープロファイル.md」としてKnowledgeに保存する
     （直接保存できなければファイルで出力して、追加を依頼する）。
   - ある場合はその内容を前提に進める。私から依頼がない限り、再ヒアリングはしない。
2. リポジトリの profile/AIキャラ設定.md の役割・話し方で対応する。

棋譜の解析が必要になったら：
- git clone https://github.com/hassan83/shogi_with_AI.git で取得し、CLAUDE.md と README.md に従う
- cd shogi_with_AI/scripts && bash shogi_setup.sh で環境構築する（「詰将棋エンジン: OK」まで出ることを確認）
- Knowledge の棋譜を kifu/ に、過去の集計結果を docs/ にコピーしてから解析する
- 更新した集計結果は、ファイルとして出力して私に渡す（私がKnowledgeに追加する）
```

### A-4. 棋譜を Knowledge に追加する

- 振り返りたい棋譜（`.kif`）をプロジェクトの Knowledge にアップロードする
- ファイル名は `g01.kif` `g02.kif` … のように通し番号にしておくと、会話で「g03を振り返りたい」と指定しやすい
- COM戦（ソフト相手）の棋譜は、人間相手と区別できる名前にしておく（例：`com_01.kif`）

### A-5. 最初の会話

プロジェクト内で新しい会話を始め、たとえば次のように話しかける。

> はじめまして。棋譜の振り返りをお願いしたいです。

1. **ヒアリング**：AIが、棋力・目標・取り組みペース・棋譜上の自分の名前などを数項目ずつ聞いてくる。
   答えたくない項目は「非公開」でよい
2. **プロファイルの保存**：AIがまとめた内容を確認し、`ユーザープロファイル.md` を Knowledge に追加する
   （AIがファイルとして出力した場合は、それをダウンロードして Knowledge にアップロードする）
3. **環境構築**：棋譜を解析するときに、AIがリポジトリを取得して `shogi_setup.sh` を実行する。
   3〜6分かかる。最後に `詰将棋エンジン: OK（1手詰めを検出）` と出れば成功

### A-6. 2回目以降の使い方

- 新しい棋譜を Knowledge に追加し、「g05を振り返りたい」のように頼む
- ヒアリングは繰り返されない。棋力や目標が変わったときは「プロファイルを更新したい」と伝える
- **会話が変わると、AIの作業環境（解析エンジン・集計結果）はリセットされる。**
  - 環境構築は、解析が必要な会話のたびにAIが行う
  - 集計結果（`対局指標.csv` など）を続けて使いたい場合は、AIから受け取ったファイルを Knowledge に追加しておく

---

## B. 手元の Linux で使う

### B-1. 動作環境

- Linux（x86-64）、または Windows の WSL2（Ubuntu など）
  - macOS・ARM（Apple Silicon、Raspberry Pi など）では、エンジンのビルド設定が合わないため動かない
- 次のコマンドが使えること：`git` `make` `g++` `python3` `pip3` `curl` `7z`
  - Ubuntu の場合の例：
    ```bash
    sudo apt-get update
    sudo apt-get install -y git build-essential python3 python3-pip curl p7zip-full
    ```
- ディスクの空き：約300MB（エンジンのソースとビルド2つ分、評価関数）
- **作業ディレクトリ `/home/claude` が必要**（エンジンと評価関数はここに置かれる。現状は固定）
  ```bash
  sudo mkdir -p /home/claude
  sudo chown "$USER" /home/claude
  ```

### B-2. 取得と環境構築

```bash
git clone https://github.com/hassan83/shogi_with_AI.git
cd shogi_with_AI
cd scripts && bash shogi_setup.sh && cd ..    # scripts/ の中から実行。3〜6分
```

最後に次のように出れば成功。

```
    詰将棋エンジン: OK（1手詰めを検出）

セットアップ完了。
```

### B-3. 棋譜を置いて解析する

```bash
mkdir -p kifu docs
cp ~/Downloads/g01.kif kifu/                   # 自分の棋譜を kifu/ に置く

# 指標の集計（docs/ から実行。結果は docs/ に作られる）
cd docs && python3 ../scripts/metrics.py ../kifu/g01.kif --name 自分の対局者名 && cd ..

# 1局の評価値の推移（リポジトリ直下から）
python3 scripts/analyze.py kifu/g01.kif
```

- `--name` には、棋譜の「先手：」「後手：」に書かれている**自分の名前**を指定する（必須）
- 深さを変えるときは `metrics.py ../kifu/g01.kif 20 --name …` のように、棋譜の後ろに数字を置く（既定は16）
- `kifu/` と `docs/` は `.gitignore` 済みなので、誤って commit されることはない

その他のスクリプト（悪手の再確認・推奨手の確認など）は `README.md` を参照。

---

## 困ったとき

| 症状 | 対処 |
|---|---|
| `shogi_setup.sh` が「詰将棋エンジン」の確認で失敗する | `rm -rf /home/claude/mate_build` してから、もう一度 `shogi_setup.sh` を実行する |
| `git clone` や評価関数のダウンロードが失敗する | 外部への通信（github.com）が許可されているか確認する（A の場合は claude.ai の設定） |
| `ModuleNotFoundError: No module named 'shogi'` | `pip3 install python-shogi` を実行する（環境によっては `--break-system-packages` が必要） |
| `metrics.py` が `usage: ... --name <自分の対局者名>` と出て終わる | `--name` で自分の対局者名を指定する |
| 自分が先手か後手か、逆に判定される | `--name` の値が、棋譜の「先手：」「後手：」の表記と完全に一致しているか確認する |
| `cd: /home/claude: No such file or directory` | B-1 の手順で `/home/claude` を作る |

解決しない場合は GitHub の Issue で報告する。**棋譜を貼るときは対局者名を伏せること**（[CONTRIBUTING.md](CONTRIBUTING.md)）。

## 更新

- 新しい版は [Releases](https://github.com/hassan83/shogi_with_AI/releases)、変更内容は `CHANGELOG.md` で確認できる
- A の場合：AIが毎回最新版を clone するので、作業は不要
- B の場合：リポジトリで `git pull` する。`CHANGELOG.md` に環境構築の変更があれば `shogi_setup.sh` を再実行する

## アンインストール（B の場合）

```bash
rm -rf /home/claude/YaneuraOu /home/claude/mate_build /home/claude/hao_extracted /home/claude/hao.7z
rm -rf shogi_with_AI    # 棋譜・集計結果（kifu/・docs/）も消えるので、必要なら先に退避する
```
