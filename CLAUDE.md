# CLAUDE.md

このリポジトリで作業するAI（Claude）が最初に読むファイル。**作業を始める前に、必ず次の手順を実行すること。**

## セッション開始時に必ず行うこと（省略しない）

1. **初回かどうかを判定する**：プロジェクトのKnowledgeに `ユーザープロファイル.md` があるか確認する
   （Knowledgeのファイル一覧、または `/mnt/project/` 配下）
   - **ない → 初回**。解析・助言より先に `profile/ユーザープロファイル.md` の手順でヒアリングし、
     結果を本人に確認してからKnowledgeに `ユーザープロファイル.md` として保存する
     （直接書き込めない場合はファイルとして出力し、本人に追加を依頼する。追加を確認してから本題に入る）
   - **ある → 2回目以降**。その内容を前提に進める。**本人から依頼がない限り再ヒアリングしない**
2. `profile/AIキャラ設定.md` を読み、その役割・話し方・分析の姿勢で対応する

判定と手順の詳細は `profile/ユーザープロファイル.md` の「AIへの指示」が正。

## 守ること

- 記入済みのプロファイル・棋譜・集計結果はリポジトリにcommitしない（公開リポジトリのため）。
  データは `kifu/`・`docs/` に置く（`.gitignore` 済み）
- 対戦相手の名前など、公開してはいけない情報をコード・README・コミットメッセージに書かない
- 使い方（環境構築・コマンド）は `README.md` を参照

## バージョン管理

- セマンティックバージョニング。番号の上げ方は `CHANGELOG.md` 冒頭の基準に従う
- 利用者に見える変更（スクリプト・profile/・CLAUDE.md・README の手順）を main に入れるときは、
  `CHANGELOG.md` の先頭に `## [Unreleased]` の節を作って追記しておく
- リリースは本人の指示があったときだけ行う。手順は `README.md` の「リリース手順」
  （Unreleased の節をバージョン番号に置き換え、`VERSION` を更新し、`vX.Y.Z` タグをpush）
