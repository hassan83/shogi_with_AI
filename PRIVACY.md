# プライバシーポリシー

制定日：2026年9月25日

本リポジトリ（shogi_with_AI）で配布するスクリプトと文書（以下「本ソフトウェア」）における、
個人情報・利用者データの扱いを定める。

## 1. 本ソフトウェアが収集する情報

**本ソフトウェアは、利用者の情報を収集・送信しない。**

- 棋譜の解析はすべて利用者自身の環境（PC・コンテナ）で実行される
- 解析スクリプト（`scripts/*.py`）は外部と通信しない
- 解析結果（CSV・Markdown）は利用者の環境の `docs/` に保存されるだけで、作者を含む第三者には送られない
- 利用状況の計測（アクセス解析・テレメトリ）は行わない

## 2. 環境構築時の通信

`scripts/shogi_setup.sh` は、環境構築のため次の配布元からソフトウェアを取得する。
取得時には、各配布元に対して通常の通信情報（IPアドレス等）が送られる。

| 取得するもの | 配布元 |
|---|---|
| YaneuraOu（将棋エンジン） | GitHub（github.com/yaneurao/YaneuraOu） |
| Háo 評価関数 | GitHub（github.com/nodchip/tanuki-） |
| python-shogi | PyPI（pypi.org） |
| p7zip | 利用者の環境のパッケージリポジトリ（apt） |

各配布元での情報の扱いは、それぞれのプライバシーポリシーに従う。

## 3. AI（claude.ai）と一緒に使う場合

`profile/` の手順でAIと振り返りを行う場合、次の情報を利用者自身が claude.ai のプロジェクトに保存する。

- ユーザープロファイル（棋力・目標・取り組みペース・棋譜上の対局者名など）
- 棋譜・集計結果

これらは**利用者のclaude.aiアカウント内に保存され、本リポジトリや作者には送られない**。
claude.ai での情報の扱いは、Anthropic のプライバシーポリシーに従う。
ヒアリングでは、答えたくない項目は「非公開」のままでよい。

## 4. 利用者にお願いしたいこと（第三者の情報の保護）

棋譜には、対戦相手のハンドル名など第三者の情報が含まれる。

- 棋譜や、対戦相手の名前を含む集計結果を、公開リポジトリ・Issue・Pull Request に載せないこと
  （`kifu/` と `docs/` は `.gitignore` 済みで、通常の操作では commit されない）
- 不具合報告に棋譜が必要な場合は、対局者名を伏せる（例：`先手：自分` `後手：相手`）

## 5. Issue・Pull Request について

GitHub の Issue・Pull Request・Discussion への投稿は、GitHub 上で誰でも閲覧できる状態で公開される。
個人情報は書き込まないこと。投稿された情報の扱いは GitHub のプライバシーポリシーに従う。

## 6. 改定

本ポリシーを改定した場合は、このファイルを更新し、変更内容を `CHANGELOG.md` に記録する。

## 7. 問い合わせ

本ポリシーに関する問い合わせは、GitHub の Issue で受け付ける（個人情報は書かないこと）。
